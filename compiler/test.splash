
main {
    var x = 5
    var str = "Hello World {x}!"

    x += 2

    print(str)
}

function print(object msg...) {
    Process.stdout.write(msg.mapToString().join(' '))
}

function readString(string? msg) {
    if (msg) {
        print(msg)
    }
    return Process.stdin.readLine()
}